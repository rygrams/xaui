# Theme

The Hybrid theme has the same semantic tokens, scales, configuration and hooks as Native.
Values remain numeric Native points until the Emotion renderer converts fixed lengths to
root-relative CSS units.

`tokens.gen.ts` is generated by the repository token pipeline and is never edited by hand.
