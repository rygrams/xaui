import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flexShrink: 1,
    flexBasis: 'auto',
    width: '100%',
  },
  track: {
    width: '100%',
    overflow: 'hidden',
  },
  progress: {
    height: '100%',
  },
  circleContainer: {
    justifyContent: 'center',
    alignItems: 'center',
  },
})
