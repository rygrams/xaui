---
"@xaui/native": patch
---

**BREAKING**: Replace Button `style` and `textStyle` props with `customAppearance` object for consistent styling API across components. Use `customAppearance={{ container, button, text }}` instead.
