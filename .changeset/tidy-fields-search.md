---
'@xaui/native': patch
---

Add SearchField at `@xaui/native/search-field`: a TextField with a built-in magnifier, a clear button and a search key, in two fills — a flat `primary` and a soft `secondary`, neither carrying the field shadow.
