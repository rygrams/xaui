---
"@xaui/ui-mcp": patch
---

Add MCP docs for `SizedBox`, `ConstrainedBox`, `FractionallySizedBox`, and `AspectRatio` components. Update `Container` and `SizedBox` docs with hybrid-specific props and alternative import paths.
