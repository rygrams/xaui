---
"@xaui/native": patch
---

Add `glass` variant and numeric `radius` support to Alert component; simplify AlertProps by removing AlertRadius enum and AlertCustomAppearance in favor of direct `ViewStyle`/`TextStyle` overrides.
