---
"@xaui/native": patch
---

Add consistent `fullWidth` behavior across native view layout components by applying `flexGrow: 1` and `flexShrink: 1`, add `fullWidth` support to `Surface`, and introduce a new `Center` view component in `@xaui/native/view`.
